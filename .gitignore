# Local-only tools — these live on the founder's machine, delivered as zips.
# The public repo stays bot + guide only.
agent-office/
agent-overwatch/
# dashboard/ is tracked (Claude maintains the project-brain tree across sessions);
# any other local cruft under it stays ignored.
dashboard/*
!dashboard/index.html
!dashboard/README.md
bad-apple-enterprise/
brand/

# Local secrets / build cruft
apikey.txt
__pycache__/
*.pyc
runs/
.DS_Store
